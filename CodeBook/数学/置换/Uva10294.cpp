#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const int MAXN = 1e6 + 7;
// 题意: 给出t种颜色, n颗珠子
// 手镯可以翻转,旋转, 项链只能旋转
// 问能组成多少种手镯和项链
// 思路:两种置换
int main(int argc, char const *argv[]) {
    int n, t; 
    while(~scanf("%d%d", &n, &t) && n) {
        
    }
    
    return 0;
}
