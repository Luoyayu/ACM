
///在一个坐标系中求相交的对数
#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    int n;
    while(scanf("%d",&n)!=EOF)
    {
        double a,b;
        for(int i=0;i<n;i++)
        {
            
        }
    }
}
