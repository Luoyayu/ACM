//DP 优化——单调队列优化
//未经优化的多重背包
//题意:给出n,m硬币种类,价格上限,A&C
#include<bits/stdc++.h>
using namespace std;
const int maxn = 111;
int A[maxn],C[maxn];
int main()
{
    int n,m;
    while(scanf("%d%d",&n,&m)!=EOF)
    {
        for(int i=0;i<n;i++)
            scanf("%d",&A[i]);
        for(int i=0;i<n;i++)
            scanf("%d",&C[i]);
        
        
    }
}